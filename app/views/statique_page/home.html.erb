<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link
        rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css"
        >
        <title>Page home</title>
    </head>
    <body>

        <% @gossips.each do |gossip| %>
        <div class="card mb-3">
            <div class="card-body">
                <p class="card-text">Gossip de : <%= gossip.user.first_name %> <%= link_to gossip.title, gossip_path(gossip) %></p>
            </div>
        </div>
        <% end %>

    </body>
</html>
